,>, // INPUT TWO NUMBERS
[<+>-] // increment the first bit until empty
<------------------------------------------------ // go back to first number and subtract the value of zero (ascii) to bring number back down to actual value
. // print  
